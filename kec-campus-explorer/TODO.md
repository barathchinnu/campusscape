# TODO

- [x] Expand ground plane size in `src/App.jsx`.
- [x] Implement mouse look (pointer lock yaw/pitch) and mouse-relative movement + camera orientation in `src/components/Player.jsx`.
- [x] Replace yellow capsule player with `public/models/player.glb` (keep movement + camera control).

